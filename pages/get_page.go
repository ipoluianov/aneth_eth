package pages

/*func GetPage(pageCode string) string {
	content := ""
	title := common.GlobalSiteName
	description := common.GlobalSiteDescription

	if pageCode == "index" {
		content = GetPageIndex()
	}

	if pageCode == "eth" {
		p1, _, _ := views.GetView("eth-price", title, description, "instance1", 300, false, false, false, false)
		content += p1
		p2, _, _ := views.GetView("eth-transfer-volume-per-minute", title, description, "instance2", 300, false, false, false, false)
		content += p2
		p3, _, _ := views.GetView("number-of-transactions-per-minute", title, description, "instance3", 300, false, false, false, true)
		content += p3
	}
	return content
}
*/
